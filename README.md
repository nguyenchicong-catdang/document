# document

Trong cú pháp Markdown, để tạo một ngắt dòng thủ công (Line Break) mà không muốn tạo một đoạn văn mới, bạn cần gõ 2 dấu cách (space) ở cuối dòng rồi nhấn Enter.  
trim_trailing_whitespace = false

## ctrl+shift+V => view VS code

# cai dat extension

Prettier => Format

# .editorconfig

```bash
root = true

[*]
charset = utf-8
end_of_line = lf
indent_size = 4
indent_style = space
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false

```
# tesst